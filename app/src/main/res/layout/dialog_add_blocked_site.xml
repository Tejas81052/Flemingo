<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="4dp"
    android:paddingEnd="24dp"
    android:paddingBottom="0dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:text="@string/blocked_sites_add_helper"
        android:textColor="@color/browser_hint"
        android:textSize="13sp" />

    <EditText
        android:id="@+id/add_blocked_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autofillHints="@null"
        android:background="@drawable/bg_dialog_input"
        android:gravity="top|start"
        android:hint="@string/blocked_sites_add_hint"
        android:importantForAutofill="no"
        android:inputType="textMultiLine|textUri|textNoSuggestions"
        android:lines="4"
        android:maxLines="8"
        android:minHeight="120dp"
        android:padding="14dp"
        android:scrollbars="vertical"
        android:textColor="@color/browser_text"
        android:textColorHint="@color/browser_hint"
        android:textCursorDrawable="@null"
        android:textSize="14sp" />

</LinearLayout>
